<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
<entry key="listSales">
	SELECT 
	    PAYMENT_DATE,
	    PAYMENT_METHOD,
	    PAYMENT_PRICE
	            FROM reservation
                    WHERE to_char(payment_date,'YYYY-MM') = ? 
</entry>

<entry key="listSalesBetween">
SELECT 
    PAYMENT_DATE,
    PAYMENT_METHOD,
    PAYMENT_PRICE
            FROM reservation
                    WHERE payment_date between TO_DATE(      ?     , 'YYYY-MM-DD')
                                       AND     TO_DATE(      ?     , 'YYYY-MM-DD') 
                                       ORDER BY payment_date ASC; 
</entry>								
</properties>